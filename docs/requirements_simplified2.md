# ExpertGG — Simplified 2: ставки на киберспортивные матчи

Статус: черновик требований для реализации. Формат специально сделан таким, чтобы этот файл можно было отдать любой другой модели/агенту и получить эквивалентную реализацию без дополнительных уточнений в чате.

---

## 0. Важное открытие — меняем модель данных

До этого момента `Match` в бэкенде проектировался как **дуэль между двумя пользователями приложения** (player1/player2 ищут друг друга, играют, у одного из них win/loss). Судя по новым скриншотам Figma ("Play" со списком матчей: "Malta Vibes / Counter-Strike / Entropiq vs Trasko / 2h 5min") и тексту задания — это **не дуэли между игроками**, а **ставки на реальные киберспортивные матчи**, данные о которых приходят из внешнего фида (PandaScore). Пользователь не играет сам — он делает ставку (gg-монетами) на то, какая из двух команд победит в реальном матче.

Из этого следует: старую модель `Match` (player1/player2/score1/score2) нужно переопределить под реальный киберспортивный матч, и добавить новую модель `Bet` — ставку пользователя на исход матча. `Profile.gg_balance` и `Leaderboard` логику менять не нужно, они уже корректны.

---

## 1. План работ (по шагам)

1. **Backend — модели и миграции**
   - Переопределить `Match` (см. §3) под данные PandaScore.
   - Добавить `Bet` (пользователь, матч, выбранная команда, ставка, статус, выплата).
   - Миграции, админка для обеих моделей (CRUD, чтобы куратор мог руками поправить статус матча/ставки).
2. **Backend — интеграция с PandaScore**
   - Регистрация на pandascore.co, получение API-токена (бесплатный тариф: 1000 запросов/час — с запасом хватает на опрос раз в минуту).
   - Клиент к PandaScore API (`GET /matches/upcoming`, `/matches/running`, `/matches/past`).
   - Cron job (раз в минуту): подтянуть матчи, "upsert" в `Match` по внешнему `pandascore_id`; для матчей, ставшых `finished`, определить победителя и запустить расчёт ставок.
3. **Backend — логика ставок**
   - `POST /api/bets/` — сделать ставку (валидация баланса, что матч ещё не начался).
   - Списание `stake` с `gg_balance` сразу при создании ставки.
   - При завершении матча: победившие ставки — начислить `stake * 2`; проигравшие — ничего (уже списано при ставке).
   - `GET /api/matches/` — список матчей с флагом текущей ставки пользователя (если есть).
   - `GET /api/bets/history/` — история ставок пользователя (замена/расширение текущего `matches/history/`).
4. **Mobile — экран Play**
   - Список матчей карточками (турнир, игра, команда1 vs команда2, таймер до начала/лайв).
   - Тап по команде — инлайн-разворот карточки (аккордеон, не модалка): выбранная команда подсвечивается рамкой, снизу появляется степпер ставки (`-`/`+`), numpad (0-9, "00", очистить), кнопки "Cancel" и "Vote" (жёлтая, с текстом "win N gg" — предпросмотр выигрыша = ставка × 2).
   - Одновременно развёрнута только одна карточка.
5. **Mobile — модалки** (именно модалки, а не инлайн — по явному требованию задания)
   - Успешная ставка принята.
   - Нет интернета (детект через `NetInfo`, показывать при потере соединения).
   - Недостаточно gg для ставки.
   - (опционально) уведомление о выигрыше/проигрыше, когда матч разрешился.
6. **Mobile — History**
   - Список ставок: две команды (как на Play), бейдж статуса (Win/зелёный, Lose/красный, Active/жёлтый), дата, изменение баланса (`+100 gg` / `-70 gg` / текущая ставка для Active).
7. **Mobile — Get Coins** (мокап монетизации)
   - Экран "Free Coins" с кнопкой "Get coins" (иконка видео) — по нажатию просто начисляет фиксированную сумму gg (без реальной интеграции рекламного SDK — это не требуется для задания).
   - Значок кошелька с "+" в хэдере Play/History/Account/Leaderboard — переход на этот экран.
8. **Mobile/Backend — Account (опционально, "по мере успеваемости")**
   - Просмотр: аватар, имя, email, кнопки "Edit profile"/"Logout".
   - Редактирование: аватар с "+" (смена картинки), поле имени, кнопка "Save".
   - `PATCH /api/account/me/` для сохранения изменений.
9. **Документация**
   - Зафиксировать все правки в этом файле по ходу работы (не копировать вручную из чата — просить AI сразу обновлять требования).
   - Обновить `docs/screens_and_api.md` итоговым контрактом.

---

## 2. Работа с PandaScore

- **Документация:** https://developers.pandascore.co/docs/introduction
- **Аутентификация:** заголовок `Authorization: Bearer <token>` (токен — в `.env`, не в коде и не в git).
- **Эндпоинты:**
  - `GET https://api.pandascore.co/matches/upcoming` — предстоящие матчи.
  - `GET https://api.pandascore.co/matches/running` — идущие сейчас.
  - `GET https://api.pandascore.co/matches/past` — завершённые (для определения победителя и расчёта ставок).
  - Пагинация: `page`, `per_page` (до 100).
- **Лимит бесплатного тарифа:** 1000 запросов/час — при опросе раз в минуту (60 запросов/час на каждый из трёх эндпоинтов = 180/час) укладываемся с большим запасом.
- **Cron:** системный cron / Celery beat / `django-crontab` — раз в минуту дергать все три эндпоинта, апсертить `Match` по `pandascore_id`, для новых `past`-матчей запускать резолв ставок.

---

## 3. Модели данных (Django)

```python
class Match(models.Model):
    pandascore_id = models.BigIntegerField(unique=True)
    tournament_name = models.CharField(max_length=255)
    videogame = models.CharField(max_length=100)  # "Counter-Strike", "League of Legends", ...
    team1_name = models.CharField(max_length=100)
    team1_logo_url = models.URLField(blank=True)
    team2_name = models.CharField(max_length=100)
    team2_logo_url = models.URLField(blank=True)
    scheduled_at = models.DateTimeField()
    status = models.CharField(
        max_length=20,
        choices=[("upcoming", "Upcoming"), ("running", "Running"), ("finished", "Finished"), ("cancelled", "Cancelled")],
    )
    winner_name = models.CharField(max_length=100, blank=True, null=True)  # заполняется по завершении
    bets_resolved = models.BooleanField(default=False)  # чтобы cron не пересчитал ставки дважды

class Bet(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name="bets")
    match = models.ForeignKey(Match, on_delete=models.CASCADE, related_name="bets")
    chosen_team = models.CharField(max_length=100)  # значение team1_name или team2_name на момент ставки
    stake = models.PositiveIntegerField()
    status = models.CharField(
        max_length=20,
        choices=[("active", "Active"), ("won", "Won"), ("lost", "Lost")],
        default="active",
    )
    payout = models.PositiveIntegerField(default=0)  # 0, пока не разрешилось; stake*2 при выигрыше
    created_at = models.DateTimeField(auto_now_add=True)
    resolved_at = models.DateTimeField(null=True, blank=True)

    class Meta:
        # опционально: одна активная ставка на матч на пользователя
        constraints = [
            models.UniqueConstraint(
                fields=["user", "match"],
                condition=models.Q(status="active"),
                name="one_active_bet_per_match",
            )
        ]
```

**Правила:**
- Ставку можно сделать только на матч со `status="upcoming"` (нельзя ставить на уже идущий/завершённый — если бизнес хочет разрешить live-ставки на `running`, это отдельное решение, по умолчанию делаем только на `upcoming`).
- При создании `Bet`: списать `stake` из `Profile.gg_balance` **сразу** (транзакция — списание баланса и создание ставки атомарно, чтобы не было гонки при двойном сабмите).
- При переводе `Match.status` в `finished` (из cron-джобы): определить `winner_name` по данным PandaScore, затем для каждой связанной активной `Bet`:
  - если `chosen_team == winner_name` → `status="won"`, `payout = stake * 2`, начислить `payout` на `gg_balance`.
  - иначе → `status="lost"`, `payout=0` (баланс не трогаем, стейк уже был списан при ставке).
  - выставить `bets_resolved=True` на матче, чтобы cron не повторял расчёт.

---

## 4. API-контракт (дополнение к `docs/screens_and_api.md`)

- `GET /api/matches/?status=upcoming|running` → список матчей + `user_bet` (вложенный объект текущей ставки пользователя на этот матч, либо `null`).
- `POST /api/bets/` — body `{match_id, chosen_team, stake}` → создаёт ставку или `400` с ошибкой (недостаточно баланса / матч уже начался / уже есть активная ставка на этот матч).
- `GET /api/bets/history/` → список ставок пользователя (won/lost/active), с данными матча (команды, дата, турнир) и `payout`/`stake`.
- `POST /api/account/get-coins/` → мок начисления монет за "просмотр рекламы" (фиксированная сумма, например `+50 gg`).
- `PATCH /api/account/me/` (опционально) → обновление `username`/`avatar`.

---

## 5. UI-детали по экранам (из Figma)

**Play (список матчей):**
- Карточка: название турнира (серый, по центру сверху), название игры под ним, затем ряд "Команда1 [иконка] VS [иконка] Команда2", снизу таймер-бейдж с иконкой часов (`30 sec`, `2h 5min` — обратный отсчёт до начала, либо "LIVE" для идущих).
- Тап по одной из команд → карточка разворачивается инлайн:
  - Заголовок "`<Команда>` wins"
  - Степпер ставки: `[-]` `[значение]` `[+]`
  - Числовая клавиатура: 1-9, 0, "00", кнопка очистки (⌫✕)
  - Кнопка "Cancel" — сворачивает карточку обратно, ставка не сохраняется
  - Кнопка "Vote" (жёлтая/оранжевая) — текст вида "win `<stake*2>` gg + bonus"; по нажатию — запрос `POST /api/bets/`, затем модалка успеха/ошибки.
- Одновременно развёрнута только одна карточка (открытие другой сворачивает предыдущую).
- Хэдер: заголовок "Play", справа баланс `<gg_balance> gg` + иконка кошелька с бейджем "+" → переход на "Get Coins".

**History:**
- Карточка на каждую ставку: бейдж статуса слева сверху (`Win` — зелёная рамка/текст, `Lose` — красная, `Active` — жёлтая), справа турнир+игра, ниже две команды (как на Play, с рамкой вокруг выбранной пользователем команды — синяя), дата снизу слева, изменение баланса справа снизу (`+100 gg` зелёным для Win, `-70 gg` красным для Lose, `20 gg` (текущая ставка) нейтральным для Active).

**Get Coins:**
- Хэдер с кнопкой "назад", заголовком "Get coins", балансом справа.
- Карточка по центру: заголовок "Free Coins", иконка монет, кнопка "Get coins" (с иконкой видео) — начисляет фиксированную сумму.

**Account (два состояния):**
- Просмотр: аватар (круглый), имя, email серым под именем, кнопка-аутлайн "Edit profile" (с иконкой карандаша), внизу экрана кнопка-аутлайн "Log out".
- Редактирование: аватар с синим "+" бейджем поверх (смена фото), поле ввода имени (пилюля), кнопка "Save" (сплошная синяя).

**Модалки (общие для всего приложения):**
- Нет интернета — показывать при потере сети (например, оверлей с иконкой и кнопкой "Повторить"), скрывать при восстановлении.
- Ставка принята — краткое подтверждение после успешного "Vote".
- Ошибка ставки — недостаточно средств / матч уже начался / другая ошибка сервера.

---

## 6. Открытые вопросы (нужно уточнить у куратора/по ходу работы)

1. Разрешены ли ставки на матчи со статусом `running` (в процессе), или строго только на `upcoming`?
2. Есть ли ограничение на минимальную/максимальную ставку?
3. Что означает "+ bonus" в тексте кнопки "Vote" на макете — это просто текст оформления, или подразумевается реальный бонусный множитель сверх x2?
4. Нужна ли пользователю возможность отменить уже сделанную ставку до начала матча?
5. Как получить `pandascore_id`/понять, какие турниры/игры показывать — все подряд, или только определённые (например, только Counter-Strike, как во всех скриншотах)?

---

## 7. Процесс работы дальше (по инструкции задания)

Начиная с этой итерации переходим в режим "требования → реализация → корректировки": все уточнения и правки, которые возникнут по ходу (в чате или от куратора), фиксируются прямо в этом файле (а не остаются только в переписке), чтобы файл сам по себе был достаточным техзаданием для реализации — в том числе если его отдать другой модели/агенту.
